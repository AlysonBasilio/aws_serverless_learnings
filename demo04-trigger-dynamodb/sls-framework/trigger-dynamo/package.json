{
  "name": "trigger-dynamo",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "invoke-local": "sls invoke local -f heroes-insert --path requests-mock/hero.insert.json --log",
    "trigger-invoke-local": "sls invoke local -f heroes-trigger --path requests-mock/hero.trigger.json --log",
    "invoke": "sls invoke -f heroes-insert --path requests-mock/hero.insert.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/joi": "^17.1.1",
    "aws-sdk": "^2.814.0"
  }
}
